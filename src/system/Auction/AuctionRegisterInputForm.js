import { ProductInfo } from 'Components/Organisms';
import React from 'react';
const AuctionRegisterInputForm = props => {
  return <ProductInfo value={props.value} onChange={props.onChange} />;
};

export default AuctionRegisterInputForm;
